---
description: Execute one approved production WordPress or PHP implementation plan
argument-hint: "[approved-plan-source]"
---
You own one terminal MID-tier **WordPress/PHP implementation** phase in the current session. WordPress is a primary production path. `$@` must identify exactly one approved, implementation-grade plan; MID-tier ownership expresses configured implementation intent and does not change the active model. Runtime routes this command through the configured `implement` phase before expansion.

Before any edit, use `ima_context` to hydrate the source and require Serena-first activation, instructions, and memory listing. Inspect planned project files/symbols narrowly. Before editing, load `ima-security-guardrails` for applicable security constraints, `readable-code` for universal readability standards, and `functional-programmer` with `php-fp` or `php-fp-wordpress`; load `ima-delegation-contract` before using `ima_delegate`. Load only additional relevant WordPress/PHP skills, including applicable environment/domain skills such as `phpunit-wp`, `wp-ddev`, `wp-local`, `ima-forms-expert`, `ima-bootstrap`, `jquery`, `js-fp-wordpress`, `livecanvas`, or ACF guidance, when plan and project evidence require them. Consume an approved design-to-code plan like any approved plan. Visual artifacts require `vision-handoff`; do not reinterpret inaccessible images or reopen approved design decisions, and stop if no accessible image reference exists.

After `ima_context` hydration, when no resume evidence packet is already present in-session, use `ima-memory-workflow` to recall the latest VERIFIED Tier-1 lifecycle artifact(s): derive `ima-pi:taskwarrior:<project>:<uuid>` for Taskwarrior or `ima-pi:jira:<KEY>` for Jira. For a Plane source, reuse a recovered lifecycle key; if no artifact establishes one, use the documented `ima-pi:plane:<workspace>:<PROJECT>-<seq>` convention rather than treating it as automatic derivation. For `lifecycle:<lifecycle-key>`, use the supplied lifecycle key directly. For `vestige:<UUID>`, retrieve only the cited memory, recover lifecycle identity when present, then recall related Qdrant manifest/direct-detail evidence. Never substitute a Taskwarrior or Jira probe for unavailable lifecycle corpus evidence; declare prerequisites missing only after the applicable lookup is empty or insufficient.

## Manual phase source identifiers

When `$@` is an identifier, accept these canonical colon forms with space-delimited aliases: `taskwarrior:<project>:<uuid>` (`taskwarrior <project> <uuid>`), `plane:<workspace>:<PROJECT>-<seq>` (`plane <workspace> <PROJECT>-<seq>`), `jira:<KEY>` (`jira <KEY>`), `lifecycle:<lifecycle-key>` (`lifecycle <lifecycle-key>`), and `vestige:<UUID>` (`vestige <UUID>`). Pass it to `ima_context` as the closed `{ type: "reference", value: "<identifier>" }` source so it normalizes before external access. Preserve the canonical colon form in handoff pointers. For lifecycle sources, use exact Tier-1 Qdrant manifest recall plus selected direct detail. For a Plane source, reuse a recovered lifecycle key; if no artifact establishes one, use the documented `ima-pi:plane:<workspace>:<PROJECT>-<seq>` convention rather than treating it as automatic derivation. For Vestige sources, read only the cited memory then use any explicit lifecycle identity to recall Qdrant; neither path has a Vestige lifecycle fallback.

When a cycle evidence packet contains `approvedPlanArtifactId`, `approvedPlanRecordKey`, `approvedPlanContentHash`, and `approvedPlanApprovedAt`, retrieve that exact original plan record directly through Tier-1 Qdrant before editing. Verify its artifact ID, record key, content hash, lifecycle/source identity, completed lifecycle verification, and implementation-grade contract. The cycle plan approval receipt alone is not executable evidence; missing, mismatched, or incomplete original-plan evidence is a contradiction and blocks edits.

Execute only an explicit approved plan containing one bounded outcome; scope/non-goals; target files/modules/symbols or a narrow discovery boundary; required behavior and observable acceptance criteria; verification expectations; and no unresolved product, architecture, security, data-integrity, rollout, or destructive-operation decision. Tracker status or source text is not sufficient approval. If source is absent, cannot hydrate, or lacks any field, identify missing evidence, point to `/ima:plan`, and do not edit.

Summarize approved scope/non-goals, targets, acceptance criteria, and verification first. A latest verified pre-review test `DEFECTS` artifact is executable repair evidence only with the approved plan: repair every plan-bound `TEST-NNN`, record each identifier's disposition, and return completed repair work to `/ima:test`. Block rather than redesign when any defect requires new product behavior, architecture, security policy, rollout, or scope. Make only minimal plan-bound changes. For AJAX handlers, verify a nonce with `wp_verify_nonce()` or `check_ajax_referer()` before processing; privileged operations additionally authorize the caller with `current_user_can()`, while intentional public handlers do not require an authenticated capability check. Sanitize and validate all user/external input at boundaries; escape output at the final boundary for its exact HTML, attribute, URL, or JavaScript context; use `$wpdb->prepare()` or an equivalent parameterized API for every dynamic SQL value, never interpolation or concatenation. Apply the WordPress section of `ima-security-guardrails`: every PHP file declares `declare(strict_types=1);` immediately after `<?php`, and cross-plugin contracts use `do_action()` or `apply_filters()` rather than `function_exists()`. Prefer WordPress APIs and established hooks/contracts. Preserve IMA cross-plugin decoupling through actions/filters where the target project establishes it, rather than direct cross-plugin calls. Apply plugins/themes, hooks, filters, shortcodes, blocks, admin UI, REST, AJAX, cron, WP-CLI, forms, browser behavior, and test tooling only when the approved plan selects them.

When dispatched by `/ima:cycle`, finish the implementation artifact with exactly one marker: `<!-- ima-cycle outcome: phase=implementation; outcome=COMPLETED -->`; use `BLOCKED` when safe implementation is impossible.

You may use the existing `wordpress-developer` role and `ima_delegate` only for complete bounded work. Current-session ownership does not require a redundant child. Parallel writers need disjoint ownership and self-contained child briefs. Add/change tests only when the approved plan includes them. Run immediate existing verification appropriate to changed files; do not automatically enter formal test or review.

A material plan/repository conflict, missing capability, convention/architecture conflict, security/privacy/data-integrity risk, material scope expansion, irreversible operation, non-goal violation, or unsafe partial state is a contradiction. Stop before speculative work and report the contradicted statement, exact evidence, files changed, verification, why discretion cannot resolve it, the smallest decision/revision needed, and safe next action. Do not claim successful implementation.

On success, call `ima_lifecycle` with type `implementation` and inherited lifecycle identity. Persist approved outcome, scope/non-goals, changed files, decisions, verification commands/results, blockers, residual risk, deviations, prior artifact IDs, and recommended next phase. Claim completion only after verified lifecycle persistence, then stop and point to `/ima:test`, or `/ima:review` only when formal testing is explicitly inapplicable. Do not invoke `/ima:cycle`, a workflow DSL, Goose recipes, or subrecipe mechanics.
