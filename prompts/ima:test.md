---
description: Run the bounded formal test phase for one implementation artifact
argument-hint: "[implementation-source]"
---
You own one terminal MID-tier **test** phase. Runtime selects the configured `test` route before expansion. `$@` must supply approved outcome, non-goals, changed files, implementation evidence, risks, and lifecycle identity. Call `ima_context`, use Serena-first evidence, load `unit-testing` as the baseline strategy router, and load `ima-security-guardrails`, `functional-programmer`, `ima-delegation-contract`, and `ima-vision-handoff` when their bounded concerns apply.

After `ima_context` hydration, when no resume evidence packet is already present in-session, use `ima-memory-workflow` and `ima-lifecycle-contract` with the public lifecycle read pair to recover the latest VERIFIED Tier-1 lifecycle artifact(s). For the exact lifecycle key, call `ima_lifecycle_recall` at `limit: 20`. Its results are descriptors, not phase evidence: if 20 return, treat the selection as potentially overflowed and block; otherwise select every required descriptor and pass it unchanged to `ima_lifecycle_get`. Accept a prerequisite only after its complete result verifies lifecycle/source identity, phase and terminal outcome, returned `artifactId`/`recordKey`, content hash, read reference, and required implementation/test semantics. Every fresh session repeats this recall-then-get sequence; never accept a descriptor, summary, pointer, or cache alone. The pair derives checkout-pin authority or, only while genuinely unpinned, exact all-phase historical Tier-1 Qdrant authority. Pending, inaccessible, unavailable, corrupt, mismatched, incomplete, overflowed, unverifiable, or cancelled reads are `BLOCKED`; do not retry, fall back, migrate, switch providers, or mix evidence. Derive `ima-pi:taskwarrior:<project>:<uuid>` for Taskwarrior or `ima-pi:jira:<KEY>` for Jira. For a Plane source, reuse a recovered lifecycle key; if no artifact establishes one, use the documented `ima-pi:plane:<workspace>:<PROJECT>-<seq>` convention rather than treating it as automatic derivation. For `lifecycle:<lifecycle-key>`, use the supplied lifecycle key directly. For `vestige:<UUID>`, retrieve only the cited memory, recover lifecycle identity when present, then use the public pair. Never substitute a Taskwarrior or Jira probe for unavailable lifecycle corpus evidence; no `ima_corpus_*` result is lifecycle evidence here. Never use a provider-native read, generated SDK namespace, or MCP discovery.

## Manual phase source identifiers

When `$@` is an identifier, accept these canonical colon forms with space-delimited aliases: `taskwarrior:<project>:<uuid>` (`taskwarrior <project> <uuid>`), `plane:<workspace>:<PROJECT>-<seq>` (`plane <workspace> <PROJECT>-<seq>`), `jira:<KEY>` (`jira <KEY>`), `lifecycle:<lifecycle-key>` (`lifecycle <lifecycle-key>`), and `vestige:<UUID>` (`vestige <UUID>`). Pass it to `ima_context` as the closed `{ type: "reference", value: "<identifier>" }` source so it normalizes before external access. Preserve the canonical colon form in handoff pointers. For lifecycle sources, use `ima_lifecycle_recall` then selected exact `ima_lifecycle_get` detail; the pair, not this prompt, derives pin-aware or genuinely unpinned historical-Qdrant authority. For a Plane source, reuse a recovered lifecycle key; if no artifact establishes one, use the documented `ima-pi:plane:<workspace>:<PROJECT>-<seq>` convention rather than treating it as automatic derivation. For Vestige sources, read only the cited memory then use any explicit lifecycle identity through the public pair; neither path has a Vestige lifecycle fallback.

Before editing, inspect the source artifact, five Serena memories, manifests, existing tests, configured commands, and relevant environment evidence. Summarize the detected testing contract: supported levels, framework, canonical commands, naming/fixture/assertion conventions, runtime constraints, and evidence gaps. Select the smallest project-supported test level; unsupported test infrastructure is a strategy change, so report the gap and ask or block rather than install dependencies, add configuration, or create browser orchestration. Keep pure rules separate from effects and test behavior rather than implementation details. Delegate only bounded test work to `tester`; test writes are limited to named tests and explicit test support.

For WordPress behavior in scope, cover relevant nonce, capability, sanitization, escaping, prepared-SQL, REST/AJAX error, and hook-contract paths; test cross-plugin behavior through actions/filters with safe defaults. Use DDEV or Local commands only when repository evidence selects them. Separate screenshot/visual-diff interpretation from DOM/accessibility, keyboard, console/network, and viewport evidence; do not introduce unsupported visual-regression infrastructure. Do not redesign or edit production behavior: report production defects and point to `/ima:implement`. Run targeted tests then the smallest meaningful broader suite.

For each production defect, assign the next stable `TEST-NNN` identifier and record the affected acceptance criterion, reproduction command or steps, expected and actual behavior, failure evidence, known affected file/module/symbol when established, and relevant error or security path. On `DEFECTS`, persist that evidence and point to `/ima:implement <canonical-source>` for plan-bound repair; after repair, retest every preserved `TEST-NNN` plus relevant regression coverage. On `PASSED`, point to `/ima:review`.

Do not test implementation details, build deep mock chains, use real timers, network, or filesystem in unit tests, weaken assertions, or add skip markers to hide defects. End with the detected testing contract; tests or test support added or repaired; changed files; commands and results; behaviors covered; defects or blockers; evidence gaps and residual risk; phase outcome; and recommended next phase.

When dispatched by `/ima:cycle`, finish the artifact with exactly one marker: `<!-- ima-cycle outcome: phase=test; outcome=PASSED -->`; use `DEFECTS` or `BLOCKED` when the result cannot advance safely. Persist a complete `test` artifact via `ima_lifecycle` and stop; recommend `/ima:implement <canonical-source>` for `DEFECTS`, `/ima:review` for `PASSED`, and no advancement for `BLOCKED`. Do not invoke `/ima:cycle`, a workflow DSL, or automatic phase progression.
