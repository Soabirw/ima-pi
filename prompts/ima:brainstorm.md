---
description: Develop approved product requirements from an idea or evidence
argument-hint: "[source]"
---
You own the HIGH-tier, interactive **product-requirements** phase. `$@` is optional source material, never approval. Turn an idea, research, problem, incomplete criteria, Jira/file/Serena/Vestige reference, or visual reference into an approved product requirements artifact. With no source, enter guided open mode.

Use evidence before assumptions. When `$@` supplies a source, hydrate it through `ima_context` before pin-aware lifecycle recall under `ima-lifecycle-contract`. Routine user preferences are already present in Pi context from the active global `AGENTS.md` when context files are enabled; do not make a broad Vestige preference lookup. A cited `vestige:<UUID>` source remains a bounded legacy-evidence source: retrieve only it and recover an explicit lifecycle identity, then use the contract's authoritative route; Vestige is never lifecycle fallback. A durable pin requires only its verified provider-native evidence; without a pin, exact Tier-1 Qdrant history across every phase establishes historical Qdrant authority. When `$@` is empty, enter guided open mode: ask for a topic and wait before topic-specific memory research or skill loading. Keep research targeted to the named topic. Load installed skills only where the topic surfaces them: `architect` to pressure-test product shape, MVP scope, and feasibility trade-offs; `ima-memory-workflow` and `ima-lifecycle-contract` for targeted memory and lifecycle evidence; `mcp-atlassian` for Jira-key seeds; `mcp-tavily` for market or competitive context as a last resort; `mcp-context7` for capability checks; `ima-brand` and `ima-copywriting` for IMA-facing messaging ideas; and `ima-delegation-contract` with the `explore` agent for narrow read-only codebase context or the `brainstormer` agent for bounded read-only ideation evidence about options, tradeoffs, and risks. Route every image or visual artifact through `vision-handoff`. Ask guided product questions in batches of two or three. Across rounds, cover scope and explicit out-of-scope, users or personas and value, technical, organizational, regulatory, or timeline constraints, existing patterns to follow or break, risks and unknowns, and success criteria; then state assumptions and risks plainly.

Produce a complete artifact with: problem or opportunity; evidence and research; users and use cases; product outcome; scope and non-goals; business rules; product acceptance criteria; constraints and dependencies; risks; open questions; decisions; source references; and recommended next phase.

Do **not** decompose PM work, select Jira/Taskwarrior hierarchy, produce technical implementation design, edit product/repository content, run commands, or enter another phase. Show the complete artifact and wait for explicit approval. After approval, persist it through `ima_lifecycle` as lifecycle type `decision`. A rootless `decision` is a permitted lifecycle seed: its verified artifact establishes R and its canonical source establishes S. It never satisfies technical-plan selection or authorizes implementation; a distinct approved `plan` must preserve exact R/S. Emit only a pointer-style `/ima:decompose` or `/ima:plan` suggestion, and stop. HIGH-tier ownership describes judgment authority; this prompt does not claim to change the current session model.
