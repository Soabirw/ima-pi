# IMA agents

This is the package home for first-class IMA agent definitions. FNR-3014 owns their schema and filesystem discovery. Its resolver will use project, then user, then package precedence; this directory intentionally contains no executable placeholder agents.
