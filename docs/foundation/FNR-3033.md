# FNR-3033 — Pi-native operational guidance skills

## Source and approved outcome

- Jira: FNR-3033; Taskwarrior: `FNR-3007` / `5324dfcd-3f22-483e-96af-c471fe7990c1`.
- Lifecycle root: Vestige `8dcf0f0d-d6d6-44e5-a75b-d863e54d39c6`; approved plan: `d1e172af-1d17-438c-9080-8a0dcd58d51d`.
- Outcome: retain useful operational guidance while removing obsolete Goose mechanics.

## Entry points and source-to-target coverage

| Source outcome | Pi-native target | Boundary |
| --- | --- | --- |
| Goose preflight skill | `/skill:pi-preflight` | Read-only explanatory routing to existing `/ima:preflight` diagnostics |
| Goose documentation guide | `/skill:pi-doc-guide` | Installed-version Pi docs first; upstream only for explicit freshness/local gaps |
| IMA Goose guide | `/skill:ima-pi-guide` | Evidence-led installation, operation, diagnosis, architecture, and integration routing |

FNR-3025 owns executable preflight. This story adds discoverable guidance, not a second diagnostic runtime.

## Evidence policy and non-goals

Skills distinguish observed local state, `ima-pi` package behavior, installed version-matched Pi semantics, and newer upstream evidence. They cite sources actually consulted, require direct evidence for health claims, avoid secrets, and label non-obvious command risk.

No extensions, prompts, agents, model routes, dependencies, manifest, installer, MCP adapter, or runtime diagnostic code changed. The package's existing `pi.skills` declaration discovers the added directories. No source files in `../ima-goose` or `../ima-claude` changed.

## Verification and limitations

Completed implementation verification:

- `node --test tests/workflow-prompts.test.js` — PASS (21/21).
- `node --test tests/discovery.test.js` — PASS (4/4).
- `npm test` — PASS (209/209).
- `git diff --check` — PASS.

Interactive `pi -e .` acceptance was not run and remains residual risk. When exercised, it may load each `/skill:*` but must not install, authenticate, configure services, or imply provider/integration health. Guidance can drift as upstream Pi changes; installed documentation remains the default and upstream differences must be labeled.

## Rollback

This is additive. Remove the three skill directories, their exact discovery/contract assertions, this record, and the README status/entry-point section. No external state or migration rollback is needed.
