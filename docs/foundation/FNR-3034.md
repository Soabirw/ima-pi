# FNR-3034 — Document and verify installation of the complete package from Git

## Identity and approved outcome

- Jira: `FNR-3034`; Taskwarrior: `FNR-3007` / `94695a9d-fbac-4532-8bb1-b421391149a9`.
- Lifecycle: `ima-pi:taskwarrior:FNR-3007:94695a9d-fbac-4532-8bb1-b421391149a9`; root: Vestige `b7f2dbec-13cb-4a46-9005-021570da7f14`.
- Plan, implementation, test, and approved review: `25b1485b-ae25-43fa-95f2-1db412c621f7`, `a1a045a4-c479-43e5-9135-701aa6912361`, `5ceaeb5a-00d5-4fe5-a8e4-e999f5942bf2`, and `a219c232-79ea-43d4-95f4-2609ccb734c0`.
- Outcome: a clean user installs `ima-pi` through normal Pi Git distribution and finds an evidence-bounded onboarding path. `ima-goose` and `ima-claude` remain unchanged.

## Canonical installation and boundaries

```bash
pi install ssh://git@gitea.theflccc.org:2222/IMA/ima-pi.git
```

Root `package.json` declares `extensions`, `skills`, and `prompts`; Pi owns their normal discovery. `scripts/install.ts` / `npm run install:skills` remain optional cross-harness skill synchronization, never a required Pi installation step. No normal path requires ETA rendering, path rewriting, or a separate cycle executable.

## Source-to-target coverage

| Capability | Pi-native destination | Disposition and limit |
| --- | --- | --- |
| Persona, security, and FP guidance | packaged prompts, policies, and skills | command/skill; outcome preservation rather than Goose injection parity |
| Project, lifecycle, and durable memory | Serena, Vestige, Qdrant via `ima-mcp` | external integrations; each backend remains independently available |
| Workflow and handoff | `/ima:*` prompts and Vestige artifacts | command/extension; manual phases, no cycle executable |
| Agents and delegation | `agents/`, `ima_delegate` | agent; bounded authority and child sessions |
| Package resources | root `pi` manifest declaration | native Pi replacement; package/user/project precedence remains Pi-owned |
| Models and providers | `config/defaults.json` and presets | documentation/configuration; role selection does not prove provider availability |
| MCP/CLI integrations and diagnostics | `extensions/mcp.ts`, integration skills, `/ima:preflight` | extension/library; optional services degrade narrowly |
| Installation and verification | `pi install`, Node tests, clean-install acceptance | native Pi replacement/documentation; installed-version update/removal syntax must be consulted |

## Acceptance and verification evidence

| Check | Result |
| --- | --- |
| `pi --version`, `pi --help`, `pi install --help` | PASS; Pi `0.82.1` and installed-version package syntax consulted |
| `node --test tests/mcp-package.test.js` | PASS, 3/3 |
| `node --test tests/discovery.test.js` | PASS, 4/4 |
| `npm test` | PASS, 209/209 with no failures, skips, cancellations, or TODOs |
| `git diff --check` | PASS; no output |
| Disposable-home Git install/list/remove | PASS; one package registration after install and none after removal |
| Isolated offline probe | PASS for representative `/ima:*` and `/skill:*` discovery with package provenance |
| Independent review | APPROVE; no Critical, Warning, Suggestion, or `REVIEW-NNN` findings |

## Documentation coverage

`README.md` provides the visible install entry point and points to [`../guide.md`](../guide.md). The guide covers prerequisites, installation, verification, configuration/model roles, commands, agents, skills, memory responsibilities, integration boundaries, degradation, preflight/troubleshooting, installed-version updates/removal, local development, and optional synchronization. Confluence page `845611010` is updated in place as **Lesson 13: Claude Code, Goose, and Pi Workflows** with an evidence-bounded three-platform comparison.

## Limits and residual risk

The isolated preflight prompt reached an unavailable default provider before producing a diagnostic result, so this record does not claim observed preflight degradation behavior. Direct package-agent catalog discovery was not independently asserted by the probe; `tests/agents.test.js` passed in the full suite. Private Gitea access remains an environment dependency. Optional integrations and provider health must be diagnosed per capability, not inferred from package installation.

## Rollback

Revert the documentation files for this story and restore the prior Confluence page revision if necessary. Do not alter Pi settings, credentials, package registrations, or external service configuration as part of documentation rollback. Use installed-version Pi documentation for package removal.
