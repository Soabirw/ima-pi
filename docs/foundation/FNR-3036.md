# FNR-3036: Pi-native lifecycle cycle coordinator

## Source and approved outcome

This delivery implements Jira FNR-3036 for Taskwarrior project `FNR-3007`, task `6bbd7673-451e-4372-b1ad-0534f869725b`, and lifecycle `ima-pi:taskwarrior:FNR-3007:6bbd7673-451e-4372-b1ad-0534f869725b`. The approved plan is Vestige `8a972f77-e665-4052-adda-160ec6861466`, grounded in requirements `5f27af6e-7a24-4f27-91ca-254892765234`.

The final fresh rereview is **APPROVED**: REVIEW-005, REVIEW-006, and REVIEW-007 are resolved, with no unresolved Critical, Warning, or Info finding. The approved provider-free evidence is 27/27 targeted tests, 95/95 broader tests, 264/264 full-package tests, a clean `git diff --check`, and `npm pack --dry-run` with 271 package files.

## Command surface

```text
/ima:cycle start [--review-cap 0-10] [--implementation generic|js|wp] [--mode guided|autonomous] <source>
/ima:cycle status
/ima:cycle stop [--ack]
/ima:cycle resume [--autonomous|--guided|--mode guided|autonomous]
/ima:cycle close [--commit-prep]
```

Accepted sources are one Jira key or supported Jira browse URL, or one exact `taskwarrior <project> <uuid>` pair. A Story is the lifecycle unit; checklists do not create independent cycles.

New cycles default to `generic` implementation mode. Explicit `generic`, `js`, and `wp` implementation modes select `/ima:implement`, `/ima:implement-js`, and `/ima:implement-wp` respectively; all use the configured `implement` route. The implementation selection persists in cycle state. Legacy schema-v1 state without `implementationMode` normalizes to `js`; an explicitly present invalid implementation mode is rejected.

Cycle mode is a separate persisted `guided | autonomous` field. New and legacy-missing cycle modes normalize to `guided`. A guided start keeps plan approval human-gated. `start --mode autonomous` marks the plan packet `autonomousPlan: true`; the plan agent self-approves only one bounded, conflict-free, low-risk delivery unit with no unresolved product, architecture, security, rollout, or verification questions, otherwise it persists `plan BLOCKED` and stops (recommending `/ima:decompose` for multiple delivery units). `resume --autonomous` upgrades a guided cycle, and `resume --guided` downgrades it; `resume` without a mode intent preserves the durable setting. New cycles use a review cap of five; existing caches preserve their explicit cap.

## Fixed lifecycle and user gates

`/ima:cycle` coordinates only this fixed lifecycle:

```text
plan -> implement -> test -> review -> resolution/rereview when requested -> document -> close
```

It is not a generic workflow engine or configurable graph. Starting deliberately dispatches `plan`. A guided start keeps the plan human-gated and waits for explicit `resume` after accepted phase evidence. An autonomous start lets the plan agent self-approve only through the fail-safe `autonomousPlan` directive; otherwise it persists `plan BLOCKED` and stops. After an approved autonomous plan, normal phase dispatch reuses the autonomous tail only while verified lifecycle evidence leaves the state `awaiting-resume`; it stops on every block, defect, unresolved outcome, review-cap excess, or dispatch-ceiling warning. `status` only reports state. `stop` pauses the active cycle and requests Pi abort; `--ack` is required when stopping a write-capable guided phase, while autonomous stop aborts the tail without acknowledgement. `resume` explicitly dispatches the current allowed phase in guided mode. When a phase persisted an explicit `BLOCKED` outcome and its external blocker is later corrected, guided `resume` may retry that same phase while preserving the blocked evidence. Review-cap, defect, invalid-transition, and post-tracker-close blocks remain terminal and cannot be bypassed by `resume`.

A review `REQUEST_CHANGES` enters `resolution` only within the configured cap, `RESOLVED` enters `rereview`, and review/rereview approval enters `document`. A document `READY` outcome makes the cycle closeout-ready; it never closes a tracker by itself. Normal `close` requires TUI confirmation and mutates exactly the selected source tracker. `close --commit-prep` performs only read-only Git status and diff checks.

## Safety, persistence, and Phase 1 durability

For every phase, including `document`, configured phase routing occurs before prompt expansion/injection. Lifecycle evidence is accepted only when project, lifecycle, source, expected phase, receipt, and semantic recall all match and the submitted artifact contains exactly one canonical outcome marker.

Phase 1 stores cycle intent/configuration and the last-known phase in project-local `.ima-cycle/active.json`. The folder creates its own `.ima-cycle/.gitignore` containing `*`, never changes the repository-root `.gitignore`, and stores no credentials. `active.json` is a validated cache; verified Vestige lifecycle artifacts remain the authoritative phase truth.

A provisional `awaiting-evidence` snapshot persists before prompt expansion or injection. Prompt expansion, asynchronous send, handshake verification, and handshake timeout failures are advisory after that write; only a synchronous prompt-injection throw is a hard error. Buffered advanced evidence is discarded on a failed handshake, leaving the provisional snapshot for reconcile-first recovery.

On `session_start`, `status`, and `resume`, active state reconciles verified lifecycle artifacts before reporting or dispatching. A missing or corrupt cache falls back safely to the branch cache or a fresh source-derived lifecycle key. A stale cache is corrected by verified lifecycle evidence, so `/ima:new` and a fresh terminal do not create a no-cycle-running ghost.

### Root-cause mapping and deferrals

- RC1: persist-before-handshake prevents a prompt dispatch from orphaning cycle state.
- RC2: `.ima-cycle/active.json` restores cross-session intent; reconciliation corrects it from Vestige.
- RC3: implementation, test, review, resolution, and rereview prompts recall the latest verified lifecycle artifact by canonical lifecycle key when no resume packet is present.
- Concurrent cycles, new store types, and a standalone CLI remain deferred. The delivered autonomous plan gate and tail remain bounded by verified evidence, the fixed reducer, review cap, dispatch ceiling, interruption, and no-auto-close.

Before close, closeout evidence is revalidated. The coordinator performs one confirmed tracker mutation, then persists lifecycle closeout evidence. Those external writes cannot be atomic: if tracker mutation succeeds but lifecycle persistence fails, the state is `blocked-after-tracker-close` and the tracker mutation is never retried automatically.

## Provider-free verification

Provider-free tests now exercise:

- production-used cycle registration and registered Pi event handlers;
- pending lifecycle-evidence buffering;
- no append before settlement and exactly one append after successful settlement;
- failed-terminal cleanup, late-settlement refusal, and subsequent-dispatch acceptance;
- explicit retry of a phase-level `BLOCKED` outcome without reopening review-cap, defect, invalid-transition, or post-tracker-close blocks;
- guided/autonomous mode parsing, legacy normalization, the autonomous plan gate, durable round-trip, verified autonomous implement-to-document progression, blocker/defect stops, and interruption without stopped-state clobbering.

The original delivery’s approved checks and counts were:

```bash
PI_CODING_AGENT_DIR=$(mktemp -d) node --test tests/cycle.test.js     # 27 tests
PI_CODING_AGENT_DIR=$(mktemp -d) node --test tests/cycle.test.js tests/integrations.test.js tests/workflow-routing.test.js tests/config.test.js tests/workflow-prompts.test.js tests/discovery.test.js  # 95 tests
PI_CODING_AGENT_DIR=$(mktemp -d) npm test                            # 264 tests
git diff --check
npm pack --dry-run                                                    # 271 package files
```

Phase 1 durability adds four pure store tests and updates the cycle wiring contract. Phase 2 adds guided/autonomous parsing, state/store, progression, stop, and no-auto-close coverage. Phase 2 initial verification was 57/57 targeted cycle tests, 5/5 targeted store tests, and 332/332 full-package tests, with a clean `git diff --check`; `npm pack --dry-run` succeeded with 285 files and only the standard gitignore-fallback notice. The bounded REVIEW-001 resolution and APPROVED rereview recorded 63/63 targeted cycle/store tests and a 333-case full-package run with a clean `git diff --check`. A fresh full-suite validation completed 333 cases: 332 passed, one optional-dependency test was skipped, and none failed.

### Phase 3 autonomous plan addendum

Phase 3 adds the plan gate for `start --mode autonomous`: only the exact standalone `autonomousPlan: true` line inside `Cycle dispatch contract (non-negotiable):` authorizes self-approval. Matching source, lifecycle, or evidence field values are non-authoritative; guided plans remain human-gated. The planner self-approves only one bounded, conflict-free, low-risk delivery unit with no unresolved product, architecture, security, rollout, or verification questions; otherwise it persists `plan BLOCKED` and stops. An approved plan uses the existing bounded tail through `document`; tracker close remains human-gated.

Phase 3 verification passed 88 focused tests (`PI_CODING_AGENT_DIR=$(mktemp -d) node --test tests/cycle.test.js tests/workflow-prompts.test.js`), 336 full-package tests (`npm test`), and `git diff --check`. REVIEW-001 was resolved and fresh rereview approved the correction with no unresolved Critical or Warning findings. Live provider/TUI acceptance remains unexercised.

This text is supported by retrieved artifacts: plan c12f3939-9820-4b9a-a479-840802e4abf6, implementation 784150d5-a437-4f3c-8e3e-ed1133a921e5, test 7333ea14-97ac-4782-baa6-c2ab4e096be3, review eb2b640d-c4be-43b7-a66b-0981fdfb357a, resolution 736aede2-c1cc-4308-9ce2-bc2debca3dd6, and rereview 2d66e071-647d-456b-a8bd-18a424b83e19.

## Human acceptance and residual risk

Live provider/TUI acceptance has not occurred. Before tracker closeout, a human must select one real Story and exercise guided start/stop/resume, `start --mode autonomous` or `resume --autonomous`, an autonomous mid-tail stop and resume, review resolution/rereview, document, and explicit close with:

```bash
cd /home/eric/IMA/dev/ima-pi
pi -e .
```

The remaining risks are real provider streaming and event scheduling; live retries, compaction, and queued follow-ups; external Jira/Taskwarrior behavior; non-atomic tracker and lifecycle persistence; Pi's lack of a dispatch run identifier; and a post-start provider hang with no fixed timeout by design. Human acceptance remains required.

## Boundaries

This delivery adds no workflow DSL, configurable graph, arbitrary phases, parallel stories, dashboard, telemetry, daemon, database, `.goose-cycle/` state, separate executable, tracker dual-write, auto-close, or changes to `ima-goose` or `ima-claude`. Guided cycles never auto-progress. Autonomous planning self-approves only through the bounded plan gate; autonomous progression then remains limited to the verified implementation-to-document tail.
